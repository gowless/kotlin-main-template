<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kotlin_base_dev">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kotlinbasedev">
        <activity android:name=".uiactivities.BeforeMain"></activity>
        <activity android:name=".uiactivities.OneCategory" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.Kotlinbasedev.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".uiactivities.InfoOneCategory" />
        <activity android:name=".uiactivities.InfoCloak" />
        <activity android:name=".uiactivities.Info" />
        <activity android:name=".uiactivities.Splash" />
        <activity android:name=".uiactivities.Cloak" />
    </application>

</manifest>